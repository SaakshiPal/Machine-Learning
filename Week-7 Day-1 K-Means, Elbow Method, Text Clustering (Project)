----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📊 WEEK-7 | DAY-1: K-Means, Elbow Method & Text Clustering (Project)

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📌 What is Clustering?

Clustering is an **unsupervised learning** technique used to:

* Group similar data points together
* Discover hidden patterns
* No labeled data required

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🎯 Why Clustering?

Used for:

* Customer segmentation
* Document grouping
* Market analysis
* Image compression
* Recommendation systems

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🔵 1️⃣ K-Means Algorithm

--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📌 What is K-Means?

K-Means is an unsupervised algorithm that:

* Divides data into **K clusters**
* Minimizes within-cluster variance
* Groups similar points together

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🧠 How K-Means Works (Step-by-Step)

1️⃣ Choose number of clusters (K)

2️⃣ Randomly initialize K centroids

3️⃣ Assign each data point to nearest centroid

4️⃣ Recalculate centroids

5️⃣ Repeat until centroids stop changing

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📊 Objective Function

K-Means minimizes:

👉 Within-Cluster Sum of Squares (WCSS)

Lower WCSS → Better clustering

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📏 2️⃣ Elbow Method

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📌 Problem

How to choose correct K?

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🧠 Solution: Elbow Method

1️⃣ Run K-Means for multiple K values
2️⃣ Calculate WCSS for each K
3️⃣ Plot K vs WCSS
4️⃣ Find "elbow point"

Elbow point:
👉 Where WCSS decreases slowly
👉 Optimal number of clusters

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📊 Interpretation

* Before elbow → Large decrease
* After elbow → Small improvement

Choose K at elbow.

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🛠️ Project: Text Clustering

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📌 Project Objective

Cluster text documents into groups without labels.

Example:

* News articles
* Customer reviews
* Research papers

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🔄 Project Workflow

1️⃣ Load text dataset
2️⃣ Text preprocessing
3️⃣ Convert text to numeric features
4️⃣ Apply K-Means
5️⃣ Use Elbow method
6️⃣ Analyze clusters

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📦 Step 1: Import Libraries

```python
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt

from sklearn.feature_extraction.text import TfidfVectorizer
from sklearn.cluster import KMeans
```

--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📥 Step 2: Load Dataset

```python
df = pd.read_csv("data/text_data.csv")
```

Assume column:

* "text"

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🧹 Step 3: Text Preprocessing

Basic steps:

* Lowercase
* Remove punctuation
* Remove stopwords

(Handled automatically in vectorizer if needed)

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🔢 Step 4: Convert Text to Numerical Features

Using TF-IDF:

```python
vectorizer = TfidfVectorizer(stop_words='english')
X = vectorizer.fit_transform(df["text"])
```

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📏 Step 5: Elbow Method

```python
wcss = []

for i in range(1, 11):
    kmeans = KMeans(n_clusters=i, random_state=42)
    kmeans.fit(X)
    wcss.append(kmeans.inertia_)

plt.plot(range(1, 11), wcss)
plt.xlabel("Number of Clusters")
plt.ylabel("WCSS")
plt.title("Elbow Method")
plt.show()
```

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🔵 Step 6: Apply K-Means

Assume optimal K = 3

```python
kmeans = KMeans(n_clusters=3, random_state=42)
df["Cluster"] = kmeans.fit_predict(X)
```

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📊 Step 7: Analyze Clusters

Check:

* Most frequent words in each cluster
* Sample texts per cluster

--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📌 Applications of Text Clustering

* News categorization
* Customer feedback grouping
* Topic modeling
* Chatbot intent grouping
* Research document classification

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

⚠️ Challenges in Text Clustering

* High dimensionality
* Sparse data
* Choosing correct K
* Interpreting clusters

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🔄 K-Means Advantages

✔ Simple and fast
✔ Scales well to large data
✔ Easy to implement

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

❌ K-Means Limitations

❌ Must choose K manually
❌ Sensitive to initialization
❌ Not good for non-spherical clusters
❌ Sensitive to outliers

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📊 K-Means vs Hierarchical Clustering

| Feature       | K-Means | Hierarchical      |
| ------------- | ------- | ----------------- |
| Need K        | Yes     | No                |
| Speed         | Fast    | Slower            |
| Scalability   | High    | Low               |
| Visualization | Hard    | Easy (Dendrogram) |

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🧠 Best Practices

* Scale numerical features
* Use TF-IDF for text
* Try multiple K values
* Use silhouette score for validation
* Remove stopwords

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📈 Optional Improvement

Use:

* MiniBatchKMeans (for large data)
* PCA for dimensionality reduction
* t-SNE for visualization

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📊 Evaluation Metrics for Clustering

Since no labels:

* Silhouette Score
* Davies-Bouldin Index
* Calinski-Harabasz Index

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

✅ WEEK-7 DAY-1 Summary

* K-Means is unsupervised clustering algorithm
* Minimizes within-cluster variance
* Elbow method helps choose K
* Text clustering uses TF-IDF
* Useful for grouping similar documents
* Simple but powerful unsupervised method

🐍 DAY-4: Python Dictionary, Tuple & Set (Theory Notes)

--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🗂 Python Dictionary

What is a Dictionary?

A **dictionary** is a built-in Python data structure used to store data in **key–value pairs**.
It works like a real dictionary:

* **Key** → word
* **Value** → meaning

--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Key Characteristics

* Stores data as `key : value`
* **Keys must be unique**
* **Keys must be immutable** (int, str, tuple)
* **Values can be any data type**
* Dictionaries are **mutable**
* Maintains insertion order (Python 3.7+)

---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Why Use Dictionaries?

* Fast data lookup
* Easy data mapping
* Real-world representation (JSON, APIs, configs)
* Efficient searching → average **O(1)** time

--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Creating a Dictionary

Dictionaries can be created in two ways:

* Using `{ }`
* Using `dict()` constructor

```python
student = {"name": "Alice", "age": 20}
```

--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Accessing Dictionary Values

* Accessed using **keys**
* Two methods:

  * `[]` → raises error if key not found
  * `get()` → returns `None` safely

```python
student["name"]
student.get("age")
```

--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Adding & Updating Elements

* New key → adds entry
* Existing key → updates value

```python
student["city"] = "Delhi"
student["age"] = 21
```

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Removing Elements

Common removal methods:

* `del` → deletes specific key
* `pop()` → removes key and returns value
* `popitem()` → removes last inserted item
* `clear()` → removes all items

--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Iterating Through Dictionary

Dictionaries can be iterated using:

* `keys()` → all keys
* `values()` → all values
* `items()` → key–value pairs

Used heavily in data processing and APIs.

--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Nested Dictionaries

A dictionary can store another dictionary as a value.
Used in **hierarchical data**, **JSON**, **database records**.

```python
user = {
  "id": 1,
  "profile": {"name": "Bob", "age": 25}
}
```

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📦 Python Tuples

What is a Tuple?

A **tuple** is an **ordered, immutable** collection of elements.

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Key Characteristics

* Ordered
* Immutable (cannot be changed)
* Allows duplicate values
* Faster than lists
* Uses less memory

---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Why Use Tuples?

* Data safety (read-only)
* Fixed data collections
* Faster iteration
* Used as dictionary keys

---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Creating Tuples

Tuples are created using commas (parentheses optional):

```python
t = (1, 2, 3)
```

Single-element tuple **must** have a comma:

```python
t = (5,)
```

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Accessing Tuple Elements

* Indexing starts from `0`
* Supports negative indexing
* Supports slicing

```python
t[0]
t[-1]
t[1:3]
```

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Tuple Unpacking

Assign tuple elements to variables in one line.

```python
a, b, c = (1, 2, 3)
```

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Advanced Unpacking (`*`)

The `*` operator collects multiple elements into a list.

```python
a, *b, c = (1, 2, 3, 4, 5)
```

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Tuple Operations

* Concatenation (`+`)
* Repetition (`*`)
* Membership (`in`)

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Immutability Rule

* Tuple elements **cannot be modified**
* Entire tuple can be deleted using `del`

---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🧺 Python Sets

What is a Set?

A **set** is an **unordered collection of unique elements**.

---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Key Characteristics

* No duplicate elements
* Unordered
* Mutable
* No indexing
* Optimized for membership testing

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Why Use Sets?

* Remove duplicates
* Fast membership checking
* Mathematical set operations
* Data filtering

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Creating Sets

Sets can be created using:

* `{ }`
* `set()` constructor

```python
s = {1, 2, 3}
```

--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Important Notes

* Empty `{}` creates a dictionary
* Use `set()` for empty set

---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Adding & Removing Elements

* `add()` → adds element
* `remove()` → error if not found
* `discard()` → safe removal
* `clear()` → removes all elements

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Set Operations

Sets support mathematical operations:

* **Union** → combine elements
* **Intersection** → common elements
* **Difference** → unique elements
* **Symmetric Difference** → uncommon elements

Used heavily in **data analysis**.

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Membership Testing

Checking presence in a set is very fast:

* Average time complexity → **O(1)**

---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

❄️ Frozenset

### What is a Frozenset?

A **frozenset** is an **immutable set**.

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Characteristics

* Cannot add or remove elements
* Supports all read-only set operations
* Hashable → can be dictionary keys

---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Why Frozenset?

* Data integrity
* Fixed datasets
* Safe sharing between functions

---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

⚡ Performance Insight

* Sets and dictionaries use **hash tables**
* Average insert/search/delete → **O(1)**
* Worst case → **O(n)** (rare)

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

✅ DAY-4 Summary

* Dictionaries → key-value data storage
* Tuples → immutable ordered data
* Sets → unique unordered data
* Frozensets → immutable sets
* Core foundation for **Data Science & Backend**

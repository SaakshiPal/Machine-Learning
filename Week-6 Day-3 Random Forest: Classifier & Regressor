----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🌲 WEEK-6 | DAY-3: Random Forest – Classifier & Regressor

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📌 What is Random Forest?

**Random Forest** is an **ensemble learning algorithm** that:

* Combines multiple **Decision Trees**
* Uses **Bagging (Bootstrap Aggregating)**
* Improves accuracy and reduces overfitting

It can be used for:

* ✅ Classification
* ✅ Regression

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🧠 Why Random Forest?

Single Decision Trees:

* Overfit easily
* High variance

Random Forest:

* Builds many trees
* Combines their predictions
* Produces more stable results

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🔄 How Random Forest Works

1. Take random samples from dataset (with replacement)
2. Train multiple decision trees
3. At each split:

   * Use random subset of features
4. Combine predictions from all trees

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📦 Two Types of Random Forest

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

1️⃣ Random Forest Classifier

Used when:

* Output variable is categorical

Examples:

* Disease / No Disease
* Spam / Not Spam
* Fraud / Legit

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Final Prediction

* Majority voting among trees

Example:
If 100 trees predict:

* 70 → Class A
* 30 → Class B

Final prediction → Class A

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

2️⃣ Random Forest Regressor

Used when:

* Output variable is continuous

Examples:

* House price
* Sales prediction
* Rainfall amount

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Final Prediction

* Average of predictions from all trees

Example:
If predictions are:

* 100
* 110
* 105

Final output:
👉 (100 + 110 + 105) / 3

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🎯 Why Random Forest is Powerful

It reduces:

* Variance (through bagging)
* Overfitting (compared to single tree)

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📊 Key Concepts

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

1️⃣ Bootstrap Sampling

* Random sampling with replacement
* Each tree sees different data

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

2️⃣ Feature Randomness

* At each split, random subset of features selected
* Increases diversity among trees

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

3️⃣ Ensemble Voting / Averaging

* Classification → Voting
* Regression → Averaging

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

⚙️ Important Hyperparameters

| Parameter         | Meaning                                  |
| ----------------- | ---------------------------------------- |
| n_estimators      | Number of trees                          |
| max_depth         | Maximum depth of each tree               |
| min_samples_split | Minimum samples required to split        |
| min_samples_leaf  | Minimum samples in leaf node             |
| max_features      | Number of features to consider per split |

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🎚️ n_estimators

* More trees → better performance (usually)
* But slower training

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🎚️ max_depth

* Controls overfitting
* Large depth → risk of overfitting
* Small depth → underfitting

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📊 Random Forest vs Decision Tree

| Feature          | Decision Tree | Random Forest |
| ---------------- | ------------- | ------------- |
| Overfitting      | High          | Low           |
| Stability        | Low           | High          |
| Accuracy         | Moderate      | High          |
| Interpretability | High          | Lower         |

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📉 Bias-Variance Perspective

* Decision Tree → Low bias, High variance
* Random Forest → Low bias, Low variance

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📊 Feature Importance

Random Forest can:

* Rank features by importance
* Identify most influential variables

Useful for:

* Feature selection
* Model interpretation

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

⚖️ Advantages

✔ High accuracy
✔ Reduces overfitting
✔ Handles missing data
✔ Works well with large datasets
✔ Provides feature importance

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

❌ Disadvantages

❌ Less interpretable
❌ Slower than single tree
❌ Large memory usage

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📌 When to Use Random Forest?

Use it when:

* Decision tree overfits
* Need better accuracy
* Dataset is medium to large
* Want feature importance insights

Avoid when:

* Real-time very fast prediction required
* Model interpretability is critical

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📊 Real-World Applications

* Credit risk prediction
* Fraud detection
* Medical diagnosis
* Stock prediction
* Recommendation systems

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🧠 Best Practices

* Use cross-validation
* Tune hyperparameters
* Start with default values
* Check feature importance
* Avoid too many trees unnecessarily

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🔄 Random Forest vs Boosting

| Feature     | Random Forest | Boosting    |
| ----------- | ------------- | ----------- |
| Training    | Parallel      | Sequential  |
| Overfitting | Low           | Can overfit |
| Speed       | Faster        | Slower      |
| Accuracy    | High          | Very High   |

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🚀 Advanced Variants

* Extra Trees (Extremely Randomized Trees)
* Gradient Boosted Trees
* XGBoost
* LightGBM
* CatBoost

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

✅ WEEK-6 DAY-3 Summary

* Random Forest is ensemble of decision trees
* Uses bagging and feature randomness
* Works for classification & regression
* Reduces overfitting compared to single tree
* Provides feature importance
* Strong and widely used ML algorithm

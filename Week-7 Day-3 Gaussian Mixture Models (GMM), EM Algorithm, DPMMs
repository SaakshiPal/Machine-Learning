-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📊 WEEK-7 | DAY-3: Gaussian Mixture Models (GMM), EM Algorithm & DPMMs

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📌 Why Another Clustering Method?

Algorithms like **K-Means** have limitations:

❌ Assumes spherical clusters
❌ Hard clustering (one point → one cluster)
❌ Requires predefined number of clusters

To overcome these problems we use **probabilistic clustering methods** such as:

* **Gaussian Mixture Models (GMM)**
* **Expectation-Maximization (EM) Algorithm**
* **Dirichlet Process Mixture Models (DPMM)**

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🔵 1️⃣ Gaussian Mixture Models (GMM)

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📌 What is GMM?

A **Gaussian Mixture Model** assumes that data is generated from a mixture of several **Gaussian distributions (normal distributions)**.

Instead of assigning points strictly to clusters, GMM assigns **probabilities of belonging to each cluster**.

👉 It is a **soft clustering method**.

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🧠 Intuition

Imagine data generated from multiple normal distributions.

Each cluster is represented by:

* Mean (μ)
* Variance (σ²)
* Probability weight (π)

The final model is a **weighted combination of Gaussians**.

--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📊 Mathematical Representation

Probability distribution:

[
P(x) = \sum_{k=1}^{K} \pi_k \mathcal{N}(x | \mu_k, \Sigma_k)
]

Where:

* (K) → number of clusters
* (\pi_k) → weight of cluster
* (\mu_k) → mean of cluster
* (\Sigma_k) → covariance matrix

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🎯 Key Characteristics

✔ Soft clustering
✔ Models complex cluster shapes
✔ Uses probability distribution

---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📊 Example

A data point might belong to:

Cluster A → 70%
Cluster B → 30%

Instead of strict assignment.

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🔄 Difference Between K-Means and GMM

| Feature         | K-Means        | GMM               |
| --------------- | -------------- | ----------------- |
| Clustering type | Hard           | Soft              |
| Cluster shape   | Spherical      | Elliptical        |
| Assignment      | One cluster    | Probabilistic     |
| Model type      | Distance based | Probability based |

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

⚙️ 2️⃣ EM Algorithm (Expectation-Maximization)

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📌 What is EM Algorithm?

The **Expectation-Maximization (EM) algorithm** is used to estimate parameters of models with **hidden variables**.

In GMM:

* Cluster membership is hidden.

EM helps estimate:

* Means
* Covariances
* Cluster probabilities

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🔄 EM Algorithm Steps

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Step 1️⃣ Initialization

Randomly initialize:

* Means
* Covariances
* Cluster probabilities

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Step 2️⃣ Expectation Step (E-Step)

Calculate probability that each point belongs to each cluster.

[
P(cluster|data)
]

This creates **soft assignments**.

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Step 3️⃣ Maximization Step (M-Step)

Update parameters using calculated probabilities.

Update:

* Mean
* Covariance
* Cluster weights

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Step 4️⃣ Repeat

Repeat E-Step and M-Step until convergence.

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🎯 Goal of EM

Maximize **log likelihood of the data**.

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📊 EM Algorithm Summary

| Step           | Action                         |
| -------------- | ------------------------------ |
| Initialization | Start with random parameters   |
| E-Step         | Estimate cluster probabilities |
| M-Step         | Update model parameters        |
| Repeat         | Until convergence              |

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🧠 Advantages of GMM + EM

✔ Flexible cluster shapes
✔ Probabilistic interpretation
✔ Handles overlapping clusters

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

❌ Limitations

❌ Sensitive to initialization
❌ Can converge to local optimum
❌ Requires specifying number of clusters

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🧩 3️⃣ Dirichlet Process Mixture Models (DPMM)

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📌 Problem With GMM

In GMM we must choose **number of clusters K** beforehand.

But in real data:
👉 Number of clusters is often unknown.

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📌 Solution: DPMM

**Dirichlet Process Mixture Models** allow:

👉 Infinite possible clusters
👉 Model automatically determines number of clusters.

This is called **non-parametric Bayesian clustering**.

--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🧠 Intuition

Instead of fixing K:

Model decides:

* How many clusters are needed
* Based on data distribution

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🍽️ Chinese Restaurant Process Analogy

A common explanation of DPMM.

Imagine a restaurant:

* Customers = Data points
* Tables = Clusters

Each new customer:

1. Joins existing table (cluster) with probability proportional to its size
2. Or starts a new table

Thus clusters grow dynamically.

---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📊 Advantages of DPMM

✔ No need to specify number of clusters
✔ Flexible model complexity
✔ Automatically adapts to data

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

❌ Limitations

❌ Computationally expensive
❌ Harder to implement
❌ Requires Bayesian inference

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📊 Comparison Summary

| Method  | Cluster Type     | Need K? | Clustering |
| ------- | ---------------- | ------- | ---------- |
| K-Means | Distance based   | Yes     | Hard       |
| GMM     | Probabilistic    | Yes     | Soft       |
| DPMM    | Bayesian mixture | No      | Soft       |

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📌 Real-World Applications

* Image segmentation
* Speech recognition
* Document clustering
* Market segmentation
* Bioinformatics

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🧠 Best Practices

✔ Normalize data before clustering
✔ Use BIC/AIC for selecting number of clusters in GMM
✔ Use multiple initializations
✔ Visualize cluster distributions

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📊 Model Selection Metrics

For GMM we use:

* **BIC (Bayesian Information Criterion)**
* **AIC (Akaike Information Criterion)**

Lower values indicate better models.

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🚀 Practical Workflow

1️⃣ Preprocess data
2️⃣ Apply GMM clustering
3️⃣ Use EM algorithm for parameter estimation
4️⃣ Use BIC/AIC to select clusters
5️⃣ If clusters unknown → try DPMM

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

✅ WEEK-7 DAY-3 Summary

* GMM models data as mixture of Gaussian distributions
* Uses EM algorithm for parameter estimation
* Allows **soft clustering**
* More flexible than K-Means
* DPMM removes need to specify cluster number
* Useful for complex probabilistic clustering problems


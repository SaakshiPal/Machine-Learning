-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📍 WEEK-5 | DAY-7: K-Nearest Neighbors (KNN)

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📌 What is K-Nearest Neighbors (KNN)?

**K-Nearest Neighbors (KNN)** is a supervised machine learning algorithm used for:

* ✅ Classification
* ✅ Regression

It is a **lazy learning algorithm** because:

* It does not learn a model during training
* It stores the dataset
* Makes predictions only when needed

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🎯 Core Idea of KNN

To predict a new data point:

1. Choose value of **K**
2. Calculate distance between new point and all training points
3. Select **K nearest neighbors**
4. Predict based on neighbors

* For classification → Majority vote
* For regression → Average value

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🧠 Why is it Called “Lazy Learning”?

Because:

* No training phase
* All computation happens during prediction
* Stores entire dataset

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📊 KNN for Classification

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

How It Works:

1. Compute distance between test point & training data
2. Pick K nearest points
3. Count class frequency
4. Assign class with highest count

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Example:

If K = 3 and nearest neighbors are:

* Class A
* Class A
* Class B

Prediction → Class A (majority)

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📉 KNN for Regression

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Instead of majority voting:

* Take average of K nearest values

Example:
Nearest values → 10, 12, 14
Prediction → (10 + 12 + 14) / 3 = 12

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📏 Distance Metrics in KNN

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

1️⃣ Euclidean Distance (Most Common)

Used for continuous features.

```
√[(x1 - y1)² + (x2 - y2)² + ...]
```

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

2️⃣ Manhattan Distance

```
|x1 - y1| + |x2 - y2| + ...
```

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

3️⃣ Minkowski Distance

Generalized distance metric.

---

🎚️ Choosing Value of K

---

Small K (e.g., 1)

* Very sensitive to noise
* Risk of overfitting
* High variance

---

Large K

* Smooth decision boundary
* Risk of underfitting
* High bias

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Rule of Thumb

* Start with √(number of samples)
* Tune using cross-validation

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📊 Decision Boundary

* KNN can create **non-linear decision boundaries**
* Flexible model
* Good for complex datasets

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

⚠️ Feature Scaling is Important

Since KNN is distance-based:

* Features with large scale dominate
* Must apply normalization or standardization

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🔄 KNN Hyperparameters

| Parameter       | Meaning                   |
| --------------- | ------------------------- |
| n_neighbors (K) | Number of neighbors       |
| metric          | Distance metric           |
| weights         | Uniform or distance-based |

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Weights Parameter

* **Uniform** → All neighbors equal weight
* **Distance** → Closer neighbors more influence

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📊 KNN vs Other Algorithms

| Aspect           | KNN        | Logistic Regression |
| ---------------- | ---------- | ------------------- |
| Boundary         | Non-linear | Linear              |
| Training time    | Very low   | Moderate            |
| Prediction time  | High       | Low                 |
| Scaling required | Yes        | Yes                 |

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

| Aspect           | KNN                 | Decision Tree |
| ---------------- | ------------------- | ------------- |
| Interpretability | Low                 | High          |
| Scaling required | Yes                 | No            |
| Speed            | Slow for large data | Faster        |

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

✅ Advantages of KNN

✔ Simple to understand
✔ No training phase
✔ Handles multi-class classification
✔ Flexible decision boundary

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

❌ Disadvantages

❌ Slow prediction
❌ Sensitive to noise
❌ Requires feature scaling
❌ Poor performance on large datasets

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📌 Real-World Applications

* Recommendation systems
* Image recognition
* Pattern recognition
* Credit scoring
* Medical diagnosis

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🧠 Best Practices

* Always scale features
* Tune K carefully
* Use cross-validation
* Remove irrelevant features
* Use distance weighting if needed

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

⚠️ Common Mistakes

* Not scaling data
* Using very small K
* Using very large K
* Ignoring class imbalance

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🔍 When to Use KNN?

Use KNN when:

* Dataset is small
* Data is not linearly separable
* Quick baseline model needed

Avoid KNN when:

* Dataset is very large
* Real-time prediction required

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

✅ WEEK-5 DAY-7 Summary

* KNN is distance-based algorithm
* Used for classification & regression
* No training phase
* Choice of K is crucial
* Requires feature scaling
* Simple but powerful for small datasets

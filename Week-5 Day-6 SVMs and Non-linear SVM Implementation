-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

⚙️ WEEK-5 | DAY-6: Support Vector Machines (SVM) & Non-Linear SVM

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📌 What is SVM?

**Support Vector Machine (SVM)** is a supervised machine learning algorithm used for:

* ✅ Classification
* ✅ Regression

SVM tries to find the **best boundary (hyperplane)** that separates classes with the **maximum margin**.

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🎯 Goal of SVM

To find a hyperplane that:

* Separates classes correctly
* Maximizes distance from nearest data points

This distance is called the **margin**.

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🧠 Key Concepts in SVM

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

1️⃣ Hyperplane

A hyperplane is:

* A line in 2D
* A plane in 3D
* A higher-dimensional boundary in n-D

It separates different classes.

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

2️⃣ Margin

Margin = Distance between hyperplane and closest data points.

SVM chooses:
👉 Hyperplane with **maximum margin**

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

3️⃣ Support Vectors

Support vectors are:

* Data points closest to the boundary
* Critical points that define the hyperplane

Removing them changes the boundary.

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📊 Linear SVM

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

When to Use Linear SVM?

* Data is linearly separable
* Classes can be divided by straight line

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Decision Boundary Equation

```
w · x + b = 0
```

Where:

* w = weights
* x = input features
* b = bias

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🔄 Hard Margin vs Soft Margin

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Hard Margin SVM

* No misclassification allowed
* Works only for perfectly separable data
* Sensitive to outliers

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Soft Margin SVM

* Allows some misclassification
* More practical
* Controlled by **C parameter**

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🎚️ Regularization Parameter (C)

C controls:

* Trade-off between margin width and misclassification

| C Value | Effect                                  |
| ------- | --------------------------------------- |
| Large C | Small margin, less tolerance for errors |
| Small C | Larger margin, more tolerance           |

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🌈 Non-Linear SVM

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Why Non-Linear SVM?

Sometimes data:

* Cannot be separated by straight line
* Has curved or complex boundaries

Solution:
👉 Use **Kernel Trick**

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🧠 Kernel Trick

Kernel transforms data into higher dimension where:

* Data becomes linearly separable

Without explicitly computing transformation.

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📌 Common Kernels

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

1️⃣ Linear Kernel

Used for:

* Linearly separable data

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

2️⃣ Polynomial Kernel

Used when:

* Data has polynomial relationship

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

3️⃣ RBF (Radial Basis Function) Kernel

Most popular non-linear kernel.

Used when:

* Complex curved boundaries
* No prior knowledge of data structure

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

4️⃣ Sigmoid Kernel

Less commonly used
Similar to neural networks

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🎚️ Important SVM Hyperparameters

| Parameter | Meaning                                 |
| --------- | --------------------------------------- |
| C         | Regularization strength                 |
| kernel    | Type of boundary                        |
| gamma     | Controls influence of single data point |
| degree    | For polynomial kernel                   |

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

Gamma Parameter (For RBF)

| Gamma      | Effect                                |
| ---------- | ------------------------------------- |
| High gamma | Complex boundary, risk of overfitting |
| Low gamma  | Smooth boundary                       |

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📊 SVM for Regression (SVR)

SVM can also:

* Predict continuous values
* Uses epsilon-insensitive loss

Used for:

* Price prediction
* Forecasting

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

⚖️ Advantages of SVM

✔ Works well in high dimensions
✔ Effective with small datasets
✔ Robust to overfitting (with proper tuning)
✔ Handles non-linear boundaries

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

❌ Disadvantages

❌ Computationally expensive
❌ Hard to interpret
❌ Sensitive to hyperparameters
❌ Requires feature scaling

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🔍 SVM vs Logistic Regression

| Aspect             | SVM           | Logistic Regression |
| ------------------ | ------------- | ------------------- |
| Decision Boundary  | Max margin    | Probability-based   |
| Handles non-linear | Yes (kernels) | No (linear only)    |
| Speed              | Slower        | Faster              |
| Interpretability   | Lower         | Higher              |

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🔍 SVM vs Decision Tree

| Aspect           | SVM             | Decision Tree |
| ---------------- | --------------- | ------------- |
| Boundary         | Smooth          | Step-like     |
| Overfitting      | Controlled by C | High risk     |
| Scaling needed   | Yes             | No            |
| Interpretability | Medium          | High          |

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🧠 Best Practices

* Always scale features
* Tune C and gamma carefully
* Use cross-validation
* Start with RBF kernel
* Check for overfitting

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📌 Real-World Applications

* Face detection
* Text classification
* Bioinformatics
* Handwriting recognition
* Image classification

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🚀 Non-Linear SVM Implementation (Concept Flow)

1. Load dataset
2. Scale features
3. Choose kernel (RBF commonly)
4. Tune hyperparameters
5. Train model
6. Evaluate using confusion matrix & ROC-AUC

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

⚠️ Common Mistakes

* Not scaling data
* Using very high gamma
* Using large C
* Ignoring cross-validation

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

✅ WEEK-5 DAY-6 Summary

* SVM finds maximum margin hyperplane
* Support vectors define boundary
* C controls regularization
* Kernel trick enables non-linear separation
* RBF is most commonly used
* SVM powerful but needs tuning

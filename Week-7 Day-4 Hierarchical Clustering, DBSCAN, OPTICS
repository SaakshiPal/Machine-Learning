--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📊 WEEK-7 | DAY-4: Hierarchical Clustering, DBSCAN & OPTICS

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📌 Why Other Clustering Algorithms?

Algorithms like **K-Means** have limitations:

❌ Must choose number of clusters beforehand
❌ Sensitive to outliers
❌ Assumes spherical clusters
❌ Struggles with irregular shapes

To solve these problems we use:

* **Hierarchical Clustering**
* **DBSCAN**
* **OPTICS**

These methods can handle **complex cluster structures**.

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🌳 1️⃣ Hierarchical Clustering

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📌 What is Hierarchical Clustering?

Hierarchical clustering builds a **tree-like structure of clusters** called a **dendrogram**.

Clusters are created in a hierarchy:

* Small clusters merge into larger clusters
* Eventually all data becomes one cluster

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🧠 Two Types of Hierarchical Clustering

1️⃣ Agglomerative (Bottom-Up)

Most commonly used.

Process:

1. Start with each data point as its own cluster
2. Merge closest clusters
3. Repeat until one cluster remains

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

2️⃣ Divisive (Top-Down)

Process:

1. Start with all points in one cluster
2. Split clusters recursively
3. Continue until each point is separate

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📊 Linkage Methods

These determine **distance between clusters**.

| Linkage Type     | Description        |
| ---------------- | ------------------ |
| Single Linkage   | Minimum distance   |
| Complete Linkage | Maximum distance   |
| Average Linkage  | Average distance   |
| Ward Linkage     | Minimizes variance |

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🌳 Dendrogram

A **dendrogram** is a tree diagram showing cluster hierarchy.

It helps determine:
👉 Optimal number of clusters.

Cut the tree at certain height to obtain clusters.

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

✅ Advantages

✔ No need to predefine K
✔ Produces hierarchy of clusters
✔ Easy visualization with dendrogram

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

❌ Disadvantages

❌ Computationally expensive
❌ Not suitable for very large datasets
❌ Sensitive to noise

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🌌 2️⃣ DBSCAN (Density-Based Spatial Clustering)

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📌 What is DBSCAN?

**DBSCAN** groups points based on **density of data points**.

Clusters form where data points are **densely packed together**.

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🎯 Key Idea

Clusters = regions with **high density**.

Points in sparse regions are considered **noise/outliers**.

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

⚙️ Important Parameters

| Parameter   | Meaning                        |
| ----------- | ------------------------------ |
| eps (ε)     | Radius of neighborhood         |
| min_samples | Minimum points to form cluster |

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🧠 Types of Points

DBSCAN classifies points into:

1️⃣ Core Points

Points with at least **min_samples** neighbors within ε.

2️⃣ Border Points

Points near core points but with fewer neighbors.

3️⃣ Noise Points

Points that do not belong to any cluster.

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🔄 How DBSCAN Works

1. Pick an unvisited point
2. Check neighbors within radius ε
3. If neighbors ≥ min_samples → form cluster
4. Expand cluster by including nearby points
5. Repeat for remaining points

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📊 Advantages

✔ Detects arbitrary shaped clusters
✔ Identifies outliers automatically
✔ No need to specify number of clusters

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

❌ Disadvantages

❌ Sensitive to eps parameter
❌ Struggles with varying density clusters

---

# 🌐 3️⃣ OPTICS (Ordering Points to Identify Clustering Structure)

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📌 What is OPTICS?

OPTICS is an extension of DBSCAN.

It solves DBSCAN's limitation when:

👉 Clusters have **different densities**.

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🧠 Key Idea

Instead of producing clusters directly, OPTICS creates:

👉 **Cluster ordering structure**

Which can be used to extract clusters at different density levels.

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📊 Output

OPTICS produces a **reachability plot**.

Valleys in this plot indicate clusters.

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

⚙️ Important Parameters

| Parameter   | Meaning                     |
| ----------- | --------------------------- |
| eps         | Maximum neighborhood radius |
| min_samples | Minimum points per cluster  |

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🎯 Advantages

✔ Handles varying density clusters
✔ Better than DBSCAN for complex data
✔ Identifies cluster hierarchy

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

❌ Disadvantages

❌ More complex to interpret
❌ Slower than DBSCAN

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📊 Comparison of Clustering Algorithms

| Algorithm    | Need K | Handles Outliers | Cluster Shape |
| ------------ | ------ | ---------------- | ------------- |
| K-Means      | Yes    | Poor             | Spherical     |
| Hierarchical | No     | Moderate         | Flexible      |
| DBSCAN       | No     | Excellent        | Arbitrary     |
| OPTICS       | No     | Excellent        | Arbitrary     |

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🧠 When to Use Which?

| Situation                    | Best Algorithm |
| ---------------------------- | -------------- |
| Small dataset with hierarchy | Hierarchical   |
| Detect outliers              | DBSCAN         |
| Different density clusters   | OPTICS         |
| Large simple dataset         | K-Means        |

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📌 Real-World Applications

* Customer segmentation
* Image segmentation
* Fraud detection
* Geographic data analysis
* Anomaly detection

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🧠 Best Practices

✔ Normalize features before clustering
✔ Visualize clusters if possible
✔ Tune eps parameter carefully
✔ Use silhouette score for validation

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📊 Evaluation Metrics

For clustering we use:

* **Silhouette Score**
* **Davies-Bouldin Index**
* **Calinski-Harabasz Index**

Higher silhouette score → better clustering.

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🚀 Practical Workflow

1️⃣ Preprocess data
2️⃣ Apply clustering algorithm
3️⃣ Tune parameters
4️⃣ Evaluate cluster quality
5️⃣ Interpret clusters

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

✅ WEEK-7 DAY-4 Summary

* Hierarchical clustering builds cluster hierarchy using dendrograms
* DBSCAN groups points based on density and detects outliers
* OPTICS improves DBSCAN by handling varying density clusters
* These algorithms handle complex cluster shapes better than K-Means

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🔁 WEEK-9 | DAY-5: RNN, Bi-directional RNN & Time Series Forecasting

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📌 What is RNN?

**Recurrent Neural Network (RNN)** is a neural network designed for:

👉 Sequential data

Examples:

* Text
* Time series
* Speech
* Stock prices

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🎯 Why RNN?

Traditional neural networks:

❌ Cannot remember previous inputs

RNN:

✔ Maintains memory of past inputs
✔ Uses previous output to influence next output

---

🧠 Key Idea

At each time step:

```text id="rnn1"
Current Input + Previous Hidden State → Output
```

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📊 RNN Architecture

```text id="rnn2"
x1 → h1 → y1
     ↓
x2 → h2 → y2
     ↓
x3 → h3 → y3
```

Where:

* (x_t) = input
* (h_t) = hidden state (memory)
* (y_t) = output

---

🔄 Mathematical Representation

[
h_t = f(W_h h_{t-1} + W_x x_t)
]

[
y_t = W_y h_t
]

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

⚠️ Problems with RNN

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

1️⃣ Vanishing Gradient

Gradients become very small → learning stops

---

2️⃣ Exploding Gradient

Gradients become very large → unstable training

---

# 🧠 Solutions

* LSTM
* GRU
* Gradient clipping

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🔵 1️⃣ Bi-directional RNN

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📌 What is Bi-directional RNN?

Processes sequence in:

👉 Forward direction
👉 Backward direction

---

🧠 Why?

Sometimes context depends on:

* Past words
* Future words

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📊 Example

Sentence:

```text id="rnn3"
"I went to the bank"
```

Understanding "bank" depends on context.

Bi-RNN uses both directions for better understanding.

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📊 Architecture

```text id="rnn4"
Forward RNN  →
Backward RNN ←
Combine Outputs
```

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

✅ Advantages

✔ Better context understanding
✔ Improved accuracy

---

❌ Disadvantages

❌ Higher computation
❌ Not suitable for real-time prediction

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📈 2️⃣ Time Series Forecasting with RNN

-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📌 What is Time Series Forecasting?

Predict future values based on past data.

Examples:

* Stock prices
* Weather prediction
* Sales forecasting

---

🧠 Why RNN for Time Series?

✔ Captures sequential dependencies
✔ Learns patterns over time

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🔄 Workflow

```text id="rnn5"
Time Series Data
      ↓
Preprocessing
      ↓
Create Sequences
      ↓
Train RNN Model
      ↓
Predict Future Values
```

---

📊 Example Dataset

```text id="rnn6"
Day → Sales
1 → 100
2 → 120
3 → 130
...
```

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

⚙️ TensorFlow Implementation

---

```python id="rnn7"
from tensorflow import keras
from tensorflow.keras import layers

model = keras.Sequential([
    layers.SimpleRNN(50, activation='relu', input_shape=(timesteps, features)),
    layers.Dense(1)
])

model.compile(
    optimizer='adam',
    loss='mse'
)

model.fit(X_train, y_train, epochs=10)
```

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🔥 LSTM Alternative (Better)

---

```python id="rnn8"
model = keras.Sequential([
    layers.LSTM(50, activation='relu', input_shape=(timesteps, features)),
    layers.Dense(1)
])
```

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🔧 PyTorch Implementation

---

```python id="rnn9"
import torch
import torch.nn as nn

class RNNModel(nn.Module):
    def __init__(self):
        super().__init__()
        self.rnn = nn.RNN(input_size=1, hidden_size=50, batch_first=True)
        self.fc = nn.Linear(50, 1)

    def forward(self, x):
        out, _ = self.rnn(x)
        out = self.fc(out[:, -1, :])
        return out
```

---

📊 Evaluation Metrics

* MSE (Mean Squared Error)
* RMSE
* MAE

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📉 RNN vs LSTM vs GRU

| Model | Memory     | Performance |
| ----- | ---------- | ----------- |
| RNN   | Short-term | Low         |
| LSTM  | Long-term  | High        |
| GRU   | Medium     | High        |

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🧠 Best Practices

✔ Normalize data
✔ Use sliding window for sequences
✔ Use LSTM for better performance
✔ Tune sequence length
✔ Use dropout

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

⚠️ Common Mistakes

❌ Not reshaping data properly
❌ Using RNN instead of LSTM
❌ Ignoring sequence order
❌ Overfitting

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

🚀 Real-World Applications

* Stock market prediction
* Weather forecasting
* Speech recognition
* Chatbots
* Machine translation

----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

📌 WEEK-9 DAY-5 Summary

* RNN handles sequential data
* Uses hidden state as memory
* Bi-directional RNN improves context
* Time series forecasting predicts future values
* LSTM/GRU solve RNN limitations
